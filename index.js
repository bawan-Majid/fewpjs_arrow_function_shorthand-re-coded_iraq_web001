let multiply = () => {
  return 5*100;
}
divide();

let square = (x) => {
  return x* 2;
}
square(5);

let add = (x,y) => {
  return x + y;
}
add(9,12);

let subtract = (x,y) => {
  return x - y;
}
add(8,3);