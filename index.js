let multiply = () => {
  return 5*100;
}
multiply();

let square = (x) => {
  return x* 2;
}
square(5);

let add = (x,y) => {
  return x + y;
}
add(9,12);

let subtract = (x,y) => {
  return x - y;
}
subtract(8,3);